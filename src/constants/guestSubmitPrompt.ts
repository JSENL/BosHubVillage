/** localStorage: user dismissed the signed-out submit spotlight */
export const GUEST_SUBMIT_PROMPT_DISMISSED_KEY = 'hubvillage_guest_submit_prompt_dismissed';

/** Nav / drawer element highlighted by {@link GuestSubmitHighlight} */
export const GUEST_SUBMIT_TARGET_SELECTOR = '[data-guest-submit-target="submit"]';
