{
  "version": 2,
  "framework": null,
  "buildCommand": "npm run build:serverless-ssr",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/sitemap\\.xml", "dest": "/api/sitemap" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/api/ssr" }
  ]
}
