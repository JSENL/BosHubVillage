{
  "version": 2,
  "framework": null,
  "buildCommand": "npm run build:serverless-ssr",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/api/ssr" }
  ]
}
