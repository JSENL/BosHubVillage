{
  "version": 2,
  "framework": null,
  "buildCommand": "npm run build:serverless-ssr",
  "outputDirectory": "dist",
  "functions": {
    "api/ssr.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/api/ssr" }
  ]
}
